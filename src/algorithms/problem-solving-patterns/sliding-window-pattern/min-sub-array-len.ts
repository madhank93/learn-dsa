/**
 * Write a function which takes a two parameters - an array of number and a positive integer
 * and it returns a minimal length of a continuos subarray of which the sum is greater than
 * or equal to the integer that has been passed. If not found return o instead
 *
 * @param arr Integer array
 * @param num a positive integer number
 */
function minSubArrayLen(arr: number[], num: number) {}
